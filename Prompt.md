# Cart Skeleton Loader Fix

The skeleton loader in the cart page doesn't match the actual CartProductCard layout. Please update the skeleton to match the exact structure, spacing, padding, and element positions of the real component to prevent layout shift.

---

# CI/CD Pipeline

To run automated quality checks and fix issues, use any of these commands:

- "run CI/CD"
- "check and fix"
- "run checks"
- "run pipeline"
- "check everything"

The pipeline will automatically: install dependencies → type-check → lint → test → build

# Write Test Cases

Write comprehensive test cases for [FILE_PATH] following all rules in `@testing-standards.mdc`.

Ensure minimum 70% coverage (branches, functions, lines, statements) and cover all rendering, user interactions, async operations, and error handling scenarios.
