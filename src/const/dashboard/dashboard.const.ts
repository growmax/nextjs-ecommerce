import { ChartConfig } from "@/components/ui/chart";

export const chartConfig = {
  orders: {
    label: "Orders",
    color: "var(--chart-1)",
  },
  quotes: {
    label: "Quotes",
    color: "var(--chart-2)",
  },
} satisfies ChartConfig;
