import { BrandsSkeleton } from "./BrandsSkeleton";

/**
 * Loading state for All Brands page
 * This displays between the global loader and the actual page content
 */
export default function BrandsLoading() {
  return <BrandsSkeleton />;
}
